// const EventToFilterMap = {
//   Everything: (events) => events.filter((event) => event).length,
// };

// export const generateFilter = (events) => {
//   return Object.entries(EventToFilterMap).map(([filterName, countPoints]) => {
//     return {
//       name: filterName,
//       count: countPoints(events),
//     };
//   });
// };
